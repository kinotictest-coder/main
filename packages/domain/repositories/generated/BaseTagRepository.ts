import { EntityRepository, type IEntitiesRepository } from '@kinotic-ai/persistence'
import { Tag } from '../../model/Tag.js'


/**
 * Base Repository for interacting with Tag entities
 * This class was generated by the Kinotic CLI. And will be overwritten if the CLI is run again.
 */
export class BaseTagRepository extends EntityRepository<Tag> {

  private readonly shouldValidate: boolean

  constructor(shouldValidate: boolean = true, entitiesRepository?: IEntitiesRepository) {
    super('minds-ignited', 'todo-neon', 'Tag', entitiesRepository)
    this.shouldValidate = shouldValidate
  }

  protected override async beforeSaveOrUpdate(entity: Tag): Promise<Tag> {
    if (this.shouldValidate) {
      return this.validate(entity)
    } else {
      return entity
    }
  }

  protected override async beforeBulkSaveOrUpdate(entities: Tag[]): Promise<Tag[]> {
    if (this.shouldValidate) {
      const validatedEntities: Tag[] = []
      for (let entity of entities) {
        validatedEntities.push(this.validate(entity))
      }
      return validatedEntities
    } else {
      return entities
    }
  }

  validate(entity: Tag): Tag {
    let ret: any
    if (entity) {
      ret = (ret ? ret : {})
      ret.id = entity.id
      ret.name = entity.name
      ret.color = entity.color
      ret.createdAt = entity.createdAt
      ret.updatedAt = entity.updatedAt
    }

    return ret
  }

}
